---
title: "Supabase MCP"
description: "建表、查询、配置一站式，支持 RLS"
maintainer: "Supabase 社区"
tier: "recommended"
installCommand: "npx -y @supabase/mcp --read-only"
whoNeeds: "用 Supabase 做后端的团队"
---

直接在 AI 对话中操作 Supabase 项目：建表、查询数据、管理配置、部署 Edge Functions。支持 Row Level Security，不绕过 Auth。

生产环境建议开启 `--read-only` 模式。
